# assets/fonts —— 内嵌中文字体

`.txt` 转 PDF 时需要一个确定存在的中文字体。不能依赖"系统里装了微软雅黑"—— 店铺机装了什么字体不受我们控制。

放一份**授权允许再分发**的开源中文字体（如思源宋体 / 思源黑体 的 OTF/TTF）。

字体文件有授权限制，不进 git 仓库（见 `.gitignore`）。放置后在这里记下字体名和许可证。

| 文件 | 字体 | 许可证 |
|---|---|---|
| （待放置） | | |
